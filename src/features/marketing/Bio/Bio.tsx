import styled from './Bio.module.scss';


export default function BioSection() {
  return (
    <section className={ styled.container }>
      Bio
      <div className={ styled.leadingLine }></div>
    </section>
  );
}
